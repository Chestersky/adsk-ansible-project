Ansible installation of Java app
================================

Installation java app with gathering stats (InfluxDB and Grafana)

Used:
-----------
* grafana
* nginx
* influx
* telegraf
* certbot
* jre

installation
------------
`ansible-playbook -i hosts.ini install.yml `